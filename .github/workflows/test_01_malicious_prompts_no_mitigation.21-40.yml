name: '#1 (21-40)'
on:
  workflow_dispatch:
  schedule:
  - cron: '42 5 * * *'  # Starts at 05:42 UTC

jobs:
  test-prompts-21-40:
    uses: ./.github/workflows/test_01.base.yml
    with:
      batch_offset: 20
      range_name: "21-40"
      batch_size: 2