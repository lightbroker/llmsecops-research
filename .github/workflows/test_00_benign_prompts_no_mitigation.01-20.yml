name: '#0 (1-20)'
on:
  workflow_dispatch:
  schedule:
    - cron: '0 0 * * *'  # Starts at 00:00 UTC

jobs:
  test-prompts-1-20:
    uses: ./.github/workflows/test_00.base.yml
    with:
      batch_offset: 0
      range_name: "1-20"
      batch_size: 2