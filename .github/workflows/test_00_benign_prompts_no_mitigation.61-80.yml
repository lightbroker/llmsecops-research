name: '#0 (61-80)'
on:
  workflow_dispatch:
  schedule:
  - cron: '51 2 * * *'  # Starts at 02:51 UTC

jobs:
  test-prompts-61-80:
    uses: ./.github/workflows/test_00.base.yml
    with:
      batch_offset: 60
      range_name: "61-80"
      batch_size: 2