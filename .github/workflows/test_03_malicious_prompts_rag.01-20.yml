name: '#3 (1-20)'
on:
  workflow_dispatch:
  schedule:
  - cron: '15 14 * * *'  # Starts at 14:15 UTC

jobs:
  test-prompts-1-20:
    uses: ./.github/workflows/test_03.base.yml
    with:
      batch_offset: 0
      range_name: "1-20"
      batch_size: 2