name: '#3 (41-60)'
on:
  workflow_dispatch:
  schedule:
   - cron: '9 16 * * *'  # Starts at 16:09 UTC

jobs:
  test-prompts-41-60:
    uses: ./.github/workflows/test_03.base.yml
    with:
      batch_offset: 40
      range_name: "41-60"
      batch_size: 2