name: '#3 (61-80)'
on:
  workflow_dispatch:
  schedule:
  - cron: '6 17 * * *'  # Starts at 17:06 UTC

jobs:
  test-prompts-61-80:
    uses: ./.github/workflows/test_03.base.yml
    with:
      batch_offset: 60
      range_name: "61-80"
      batch_size: 2